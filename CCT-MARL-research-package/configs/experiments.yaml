experiments:
  e2_shielded_baselines:
    methods: [MAPPO+Shield, IQL+Shield, Greedy, Greedy-Reservation, CCT-MARL]
    seeds: [0, 1, 2, 3, 4]
    metrics: [Return, Makespan, Waiting, Collision, Rework, Bits]
  e3_scalability:
    settings:
      - grid: 8
        dyads: 5
      - grid: 10
        dyads: 8
      - grid: 20
        dyads: 10
        status: optional_sanity
  e4_communication:
    variants: [no_aggregation, mean_aggregation]
    future_variants: [attention_aggregation, auxiliary_experience_loss]
  e5_reward_sensitivity:
    variants: [full, no_speed_bonus, no_waiting_penalty, no_collision_penalty, no_invalid_penalty]
